name: 'dbt_validation_pipeline'
version: '1.0.0'
config-version: 2

profile: 'bachelorarbeit_validation'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_validation_pipeline:
    staging:
      +materialized: view
    validation:
      +materialized: view
    reports:
      +materialized: view